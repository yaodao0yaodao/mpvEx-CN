# VIVO 调试安装自动授权

`vivo-package-installer.json5` 是供本项目开发调试使用的 GKD 本地订阅。它只在 VIVO 系统“软件包安装程序”页面运行，用于处理 ADB 安装仍被系统拦截时出现的：

- “授权本次安装”；
- 风险确认复选框与“继续安装”；
- “安装”；
- 安装完成后的“完成”。

规则不会处理“该应用存在高危风险 / 仍要继续”页面。遇到这种安全告警时仍需人工确认，以免把真正的风险告警变成无条件自动放行。

导入后需在 GKD 中保持本地订阅和上述两个规则组启用，并保持 GKD 无障碍服务运行。
